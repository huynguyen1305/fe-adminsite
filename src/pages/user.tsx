import { Divider } from "antd";
import CreateUser from "@/modules/CreateUser";

const user = () => {
  return (
    <div>
      <CreateUser />
      <Divider />
    </div>
  );
};

export default user;
